{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Delete ' ~ script %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('code-slash') ~ ' Scripts', 'script_index'),
  bootstrap_breadcrumb('Delete'),
) }}
{% endblock %}
